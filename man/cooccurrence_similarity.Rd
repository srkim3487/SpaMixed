% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cooccurrence_similarity.R
\name{cooccurrence_similarity}
\alias{cooccurrence_similarity}
\title{Construct taxon adjacency using co-occurrence or co-abundance similarity}
\usage{
cooccurrence_similarity(
  otu,
  taxa_are_rows = TRUE,
  pseudocount = 0.5,
  cor_method = c("spearman", "pearson"),
  use_abs_correlation = TRUE,
  threshold = NULL,
  quantile_prob = 0.9,
  top_k = NULL,
  sym_rule = c("union", "mutual")
)
}
\arguments{
\item{otu}{A phyloseq object, otu_table, matrix, or data.frame.}

\item{taxa_are_rows}{Logical. Used only when \code{otu} is a matrix or data.frame.
If TRUE, rows are taxa and columns are samples.}

\item{pseudocount}{Numeric pseudocount added before CLR transformation.}

\item{cor_method}{Correlation method. Either "spearman" or "pearson".}

\item{use_abs_correlation}{Logical. If TRUE, uses absolute correlations,
capturing both co-occurrence and co-exclusion strength. If FALSE, only
positive correlations are used.}

\item{threshold}{Optional numeric threshold for similarity. If provided, taxa with similarity
greater than or equal to this value are connected.}

\item{quantile_prob}{Numeric quantile used to define the similarity threshold
when \code{threshold} is NULL and \code{top_k} is NULL. Default is 0.9, corresponding
to connecting taxon pairs in the top 10\% of pairwise similarities.}

\item{top_k}{Optional integer. If provided, each taxon is connected to its
top-k most similar taxa before symmetrization.}

\item{sym_rule}{Symmetrization rule used when \code{top_k} is provided. Either "union" or "mutual". Default is `"union".}
}
\value{
A list containing:
\describe{
\item{similarity}{Taxon-by-taxon similarity matrix.}
\item{correlation}{Taxon-by-taxon CLR correlation matrix.}
\item{adj_micro}{Binary taxon adjacency matrix.}
\item{counts}{Taxa-by-samples count matrix used in the calculation.}
\item{method}{Description of the similarity method.}
}
}
\description{
Constructs a taxon-level similarity matrix using CLR-transformed abundance
correlations and converts it into a binary adjacency matrix.
}
\details{
The OTU table is first transformed using a centered log-ratio (CLR)
transformation after adding a pseudocount. Pairwise taxon correlations are
then computed across samples. If \code{use_abs_correlation = TRUE}, the absolute
value of the correlation is used, so both positive co-occurrence and negative
co-exclusion patterns contribute to the similarity matrix. If
\code{use_abs_correlation = FALSE}, only positive correlations are retained.

The binary adjacency matrix can be constructed in three ways:
\enumerate{
\item If \code{threshold} is provided, taxa with similarity greater than or equal
to \code{threshold} are connected.
\item If \code{top_k} is provided, each taxon is connected to its top-k most
similar taxa before symmetrization.
\item If neither \code{threshold} nor \code{top_k} is provided, the threshold is set
to the \code{quantile_prob} quantile of the off-diagonal similarity values. For
example, \code{quantile_prob = 0.90} connects taxa whose similarity is in the
top 10\% of all pairwise similarities.
}

When \code{top_k} is used, \code{sym_rule} controls how the directed top-k graph is
converted to an undirected adjacency matrix. If \code{sym_rule = "union"}, two
taxa are connected if either taxon is among the other's top-k most similar
taxa. If \code{sym_rule = "mutual"}, two taxa are connected only if each taxon is
among the other's top-k most similar taxa. The \code{"union"} rule is less
restrictive and usually gives a denser graph, whereas \code{"mutual"} is stricter
and gives a sparser graph. The \code{sym_rule} argument is ignored when a
threshold-based adjacency matrix is used.
}
\examples{

library(SpaMixed)

# Load example data included in the package
data(physeq_example)

cooc_res <- cooccurrence_similarity(
  otu = physeq_example,
  taxa_are_rows = TRUE,
  pseudocount = 0.5,
  cor_method = "spearman",
  use_abs_correlation = TRUE,
  top_k = 5,
  sym_rule = "union"
)

adj_micro_cooc <- cooc_res$adj_micro
dim(adj_micro_cooc)

}
\seealso{
\code{\link{realized_niche_similarity}},
\code{\link{environmental_niche_similarity}},
\code{\link{phylogenetic_similarity}},
\code{\link{region_adjacency_matrix}},
\code{\link{fit_SpaMixed}}
}
